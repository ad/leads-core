{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Create Form Request",
  "description": "Schema for creating a new form",
  "required": ["name", "type"],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 255,
      "description": "The name of the form"
    },
    "type": {
      "type": "string",
      "enum": ["contact", "lead", "survey", "feedback", "newsletter"],
      "description": "The type of the form"
    },
    "enabled": {
      "type": "boolean",
      "default": true,
      "description": "Whether the form is enabled"
    },
    "fields": {
      "type": "object",
      "description": "Form field definitions",
      "patternProperties": {
        "^[a-zA-Z_][a-zA-Z0-9_]*$": {
          "type": "object",
          "required": ["type"],
          "properties": {
            "type": {
              "type": "string",
              "enum": ["text", "email", "tel", "textarea", "select", "checkbox", "radio", "number", "url", "date"]
            },
            "required": {
              "type": "boolean",
              "default": false
            },
            "placeholder": {
              "type": "string"
            },
            "options": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "validation": {
              "type": "object",
              "properties": {
                "minLength": {"type": "integer", "minimum": 0},
                "maxLength": {"type": "integer", "minimum": 1},
                "pattern": {"type": "string"}
              }
            }
          }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
